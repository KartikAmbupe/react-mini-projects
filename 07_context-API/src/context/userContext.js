import React from "react";

const UserContext = React.createContext(); //creating a context
//this will create a context object that we can use to provide and consume values



export default UserContext;